// src/lib/sector-configs/custom.ts
// Secteur custom — aucune feature par défaut, tout activable manuellement
import type { SectorTheme } from "@/lib/sector-theme";

export const customDefaultFeatures: string[] = [];

export const customTheme: SectorTheme = {
  primary: "#374151",
  accent: "#6B7280",
  bg: "#F9FAFB",
  label: "Personnalisé",
};